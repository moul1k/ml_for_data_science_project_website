(() => {const buttons=[...document.querySelectorAll('[data-go]')];const pages=[...document.querySelectorAll('[data-page]')];const names=pages.map(x=>x.id);document.documentElement.classList.add('js');function show(name,scroll=true){if(!names.includes(name))name='Introduction';pages.forEach(x=>{const selected=x.id===name;x.classList.toggle('visible',selected);x.hidden=!selected});buttons.forEach(x=>{const selected=x.dataset.go===name;x.classList.toggle('active',selected);if(selected)x.setAttribute('aria-current','page');else x.removeAttribute('aria-current')});if(scroll)document.querySelector('#research').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'})}buttons.forEach(x=>x.addEventListener('click',()=>{location.hash=x.dataset.go;if(location.hash.slice(1)===x.dataset.go)show(x.dataset.go)}));window.addEventListener('hashchange',()=>show(decodeURIComponent(location.hash.slice(1))));document.querySelector('#overview').addEventListener('click',()=>document.querySelector('#system').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'}));show(decodeURIComponent(location.hash.slice(1)),false)})();
